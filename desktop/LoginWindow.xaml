<Window x:Class="BlocksPlant.Desktop.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Blocks Plant — Login"
        Height="420" Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F5F5F0">
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Blocks Plant" FontSize="28" FontWeight="SemiBold" Foreground="#2C3E2D"/>
        <TextBlock Grid.Row="1" Text="Office POS" FontSize="14" Foreground="#5A6B5C" Margin="0,4,0,24"/>

        <TextBlock Grid.Row="2" Text="Username" Margin="0,0,0,4"/>
        <TextBox x:Name="UsernameBox" Grid.Row="2" Margin="0,20,0,0" Height="32" Padding="6,4" FontSize="14"
                 Text="cashier" VerticalContentAlignment="Center"/>

        <TextBlock Grid.Row="3" Text="Password" Margin="0,16,0,4"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="3" Margin="0,20,0,0" Height="32" Padding="6,4" FontSize="14"
                     VerticalContentAlignment="Center"/>

        <TextBlock Grid.Row="4" Text="API Base URL" Margin="0,16,0,4"/>
        <TextBox x:Name="ApiUrlBox" Grid.Row="4" Margin="0,20,0,0" Height="28" Padding="6,4" FontSize="12"
                 VerticalContentAlignment="Center"/>

        <StackPanel Grid.Row="5" Margin="0,24,0,0">
            <Button x:Name="LoginButton" Content="Sign in" Height="36" FontSize="14" FontWeight="SemiBold"
                    Background="#3D5A40" Foreground="White" BorderThickness="0"
                    Click="LoginButton_Click" IsDefault="True"/>
            <TextBlock x:Name="ErrorText" Foreground="#A33" TextWrapping="Wrap" Margin="0,12,0,0"/>
        </StackPanel>
    </Grid>
</Window>
