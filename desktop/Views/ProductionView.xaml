<UserControl x:Class="BlocksPlant.Desktop.Views.ProductionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="420"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Style="{StaticResource PanelCard}" Margin="0,0,14,0">
            <StackPanel>
                <TextBlock Text="Production entry" Style="{StaticResource PageTitle}"/>
                <TextBlock Text="Record good blocks produced. This increases finished stock."
                           Style="{StaticResource PageSubtitle}"/>

                <TextBlock Text="Product" Style="{StaticResource FieldLabel}"/>
                <ComboBox x:Name="ProductCombo" Margin="0,0,0,12" DisplayMemberPath="Name" Height="40"/>

                <TextBlock Text="Quantity produced" Style="{StaticResource FieldLabel}"/>
                <TextBox x:Name="QtyBox" Margin="0,0,0,12" Text="0" Height="40" FontSize="16" FontWeight="SemiBold"/>

                <TextBlock Text="Rejects (optional)" Style="{StaticResource FieldLabel}"/>
                <TextBox x:Name="RejectsBox" Margin="0,0,0,16" Text="0" Height="40"/>

                <Button Content="Submit production" Height="44" Style="{StaticResource PrimaryButton}" Click="Submit_Click"/>
                <TextBlock x:Name="StatusText" Margin="0,12,0,0" TextWrapping="Wrap"
                           Foreground="{StaticResource MutedBrush}"/>
            </StackPanel>
        </Border>

        <Border Grid.Column="1" Style="{StaticResource PanelCard}" Padding="0">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="Current stock" FontWeight="SemiBold"
                           FontSize="14" Margin="16,14,16,10"/>
                <DataGrid x:Name="StockGrid" BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Product" Binding="{Binding ProductName}" Width="*"/>
                        <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="90"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
