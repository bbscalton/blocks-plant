<UserControl x:Class="BlocksPlant.Desktop.Views.RecipesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel>
        <TextBlock DockPanel.Dock="Top" Text="Product recipes (BOM)" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,8"/>
        <TextBlock DockPanel.Dock="Top"
                   Text="Quantity of each raw material consumed per good block produced. Production rejects do not consume extra material."
                   Foreground="#555" Margin="0,0,0,12" TextWrapping="Wrap"/>

        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Product" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ComboBox x:Name="ProductCombo" Width="220" DisplayMemberPath="ProductName"
                      SelectionChanged="ProductCombo_SelectionChanged"/>
            <Button Content="Reload" Margin="12,0,0,0" Click="Reload_Click"/>
        </StackPanel>

        <StackPanel DockPanel.Dock="Bottom" Margin="0,12,0,0">
            <Button Content="Save recipe" Height="36" Width="140" HorizontalAlignment="Left"
                    Background="#3D5A40" Foreground="White" BorderThickness="0" Click="Save_Click"/>
            <TextBlock x:Name="StatusText" Margin="0,8,0,0" TextWrapping="Wrap"/>
        </StackPanel>

        <DataGrid x:Name="LinesGrid" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Material" Binding="{Binding MaterialName}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Unit" Binding="{Binding Unit}" Width="80" IsReadOnly="True"/>
                <DataGridTextColumn Header="Qty per block" Binding="{Binding QuantityPerBlock, UpdateSourceTrigger=PropertyChanged}" Width="140"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
